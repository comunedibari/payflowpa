package  it.tasgroup.idp.gateway.enums;

/*
 *  INTERNO/INTERFACCIA, indica se l'evento tracciato è relativo un'operazione di interfaccia 
 *  con altri sistemi oppure se rappresenta un'operazione interna (es. cambio di stato) al proprio sistema
 */
public enum EnumCategoriaEvento {
    INTERNO,
    INTERFACCIA
}
